eureka:
  client:
    registry-fetch-inteval-seconds: 5
    service-url:
      defaultZone: http://localhost:8700/eureka/

config:
  server:
    student-keyyy: Hello There, from Config Server
